name: MAIN Flow

on:
  push:
    branches:
      - main

jobs:
  hello_job_main:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Run a one-line script
      run: echo Hello, world!